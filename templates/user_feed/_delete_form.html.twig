<form method="post" action="{{ path('app_user_feed_delete', {'id': user_feed.feed.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_feed.feed.id) }}">
    <button class="btn">Supprimer</button>
</form>
